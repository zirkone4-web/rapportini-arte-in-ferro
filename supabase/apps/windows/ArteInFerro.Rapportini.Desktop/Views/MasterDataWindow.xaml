<Window x:Class="ArteInFerro.Rapportini.Desktop.Views.MasterDataWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Clienti e cantieri · Arte In Ferro" Width="1360" Height="820"
        WindowStartupLocation="CenterOwner" Background="#F1F5F9">
    <Grid Margin="18">
        <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <TabControl>
            <TabItem Header="CLIENTI">
                <Grid Margin="12">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="14"/><ColumnDefinition Width="420"/></Grid.ColumnDefinitions>
                    <DataGrid ItemsSource="{Binding Clients}" SelectedItem="{Binding SelectedClient}"
                              AutoGenerateColumns="False" IsReadOnly="True" Background="White">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Ragione sociale" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Indirizzo" Binding="{Binding Address}" Width="260"/>
                            <DataGridTextColumn Header="Referente" Binding="{Binding Contact}" Width="160"/>
                            <DataGridTextColumn Header="Telefono" Binding="{Binding Phone}" Width="140"/>
                            <DataGridTextColumn Header="Stato" Binding="{Binding StateLabel}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Border Grid.Column="2" Background="White" CornerRadius="12" Padding="20">
                        <ScrollViewer><StackPanel>
                            <DockPanel><TextBlock Text="Anagrafica cliente" FontSize="22" FontWeight="Bold"/><Button Content="NUOVO" Command="{Binding NewClientCommand}" DockPanel.Dock="Right"/></DockPanel>
                            <TextBlock Text="Ragione sociale *" Margin="0,14,0,0"/><TextBox Text="{Binding ClientName, UpdateSourceTrigger=PropertyChanged}"/>
                            <TextBlock Text="Indirizzo *"/><TextBox Text="{Binding ClientAddress, UpdateSourceTrigger=PropertyChanged}"/>
                            <TextBlock Text="Referente"/><TextBox Text="{Binding ClientContact, UpdateSourceTrigger=PropertyChanged}"/>
                            <TextBlock Text="Telefono"/><TextBox Text="{Binding ClientPhone, UpdateSourceTrigger=PropertyChanged}"/>
                            <CheckBox Content="Cliente attivo" IsChecked="{Binding ClientActive}" Margin="0,8,0,14"/>
                            <TextBlock Text="Motivazione obbligatoria" FontWeight="SemiBold"/><TextBox Text="{Binding Reason, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" MinHeight="65" TextWrapping="Wrap"/>
                            <Button Content="SALVA CLIENTE" Command="{Binding SaveClientCommand}" Background="{StaticResource IronBlue}" Foreground="White"/>
                        </StackPanel></ScrollViewer>
                    </Border>
                </Grid>
            </TabItem>
            <TabItem Header="CANTIERI E POSIZIONI">
                <Grid Margin="12">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="14"/><ColumnDefinition Width="430"/></Grid.ColumnDefinitions>
                    <DataGrid ItemsSource="{Binding Worksites}" SelectedItem="{Binding SelectedWorksite}"
                              AutoGenerateColumns="False" IsReadOnly="True" Background="White">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Cantiere" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Cliente" Binding="{Binding ClientName}" Width="210"/>
                            <DataGridTextColumn Header="Indirizzo" Binding="{Binding Address}" Width="240"/>
                            <DataGridTextColumn Header="Raggio m" Binding="{Binding RadiusMeters}" Width="85"/>
                            <DataGridTextColumn Header="Stato" Binding="{Binding StateLabel}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Border Grid.Column="2" Background="White" CornerRadius="12" Padding="20">
                        <ScrollViewer><StackPanel>
                            <DockPanel><TextBlock Text="Scheda cantiere" FontSize="22" FontWeight="Bold"/><Button Content="NUOVO" Command="{Binding NewWorksiteCommand}" DockPanel.Dock="Right"/></DockPanel>
                            <TextBlock Text="Cliente *" Margin="0,14,0,0"/><ComboBox ItemsSource="{Binding ClientChoices}" SelectedItem="{Binding SelectedWorksiteClient}" DisplayMemberPath="Label"/>
                            <TextBlock Text="Nome cantiere *" Margin="0,9,0,0"/><TextBox Text="{Binding WorksiteName, UpdateSourceTrigger=PropertyChanged}"/>
                            <TextBlock Text="Indirizzo *"/><TextBox Text="{Binding WorksiteAddress, UpdateSourceTrigger=PropertyChanged}"/>
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="10"/><ColumnDefinition/></Grid.ColumnDefinitions>
                                <StackPanel><TextBlock Text="Latitudine *"/><TextBox Text="{Binding LatitudeText, UpdateSourceTrigger=PropertyChanged}"/></StackPanel>
                                <StackPanel Grid.Column="2"><TextBlock Text="Longitudine *"/><TextBox Text="{Binding LongitudeText, UpdateSourceTrigger=PropertyChanged}"/></StackPanel>
                            </Grid>
                            <TextBlock Text="Raggio autorizzato (metri) *"/><TextBox Text="{Binding RadiusText, UpdateSourceTrigger=PropertyChanged}"/>
                            <TextBlock Text="Note"/><TextBox Text="{Binding WorksiteNotes, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" MinHeight="55"/>
                            <CheckBox Content="Cantiere attivo" IsChecked="{Binding WorksiteActive}" Margin="0,8"/>
                            <Button Content="APRI POSIZIONE SULLA MAPPA" Command="{Binding OpenWorksiteMapCommand}"/>
                            <TextBlock Text="Motivazione obbligatoria" FontWeight="SemiBold"/><TextBox Text="{Binding Reason, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" MinHeight="60" TextWrapping="Wrap"/>
                            <Button Content="SALVA CANTIERE" Command="{Binding SaveWorksiteCommand}" Background="{StaticResource IronBlue}" Foreground="White"/>
                        </StackPanel></ScrollViewer>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
        <Border Grid.Row="1" Background="#E2E8F0" Padding="12" Margin="0,12,0,0"><TextBlock Text="{Binding Message}" TextWrapping="Wrap"/></Border>
    </Grid>
</Window>
